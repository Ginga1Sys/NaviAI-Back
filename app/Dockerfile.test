FROM maven:3.9.9-eclipse-temurin-17
WORKDIR /workspace

# Copy project
COPY . /workspace

# Pre-download dependencies for faster container startup
RUN mvn -B -DskipTests dependency:go-offline

# Default command for test container usage
CMD ["mvn", "-B", "test"]
